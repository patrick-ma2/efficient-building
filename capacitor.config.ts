import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'efficient-building-App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
